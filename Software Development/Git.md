## Comandos Básicos

```bash
git status
git add .
git commit -m 'menssage'
git push
git log
git pull
git stash
git config --local user.name 'your name'
git config --local user.email 'your email'
```

